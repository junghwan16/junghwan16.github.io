---
layout: single
title: "OpenRTB BidResponse 구조 완전 정리"
date: 2026-02-24 15:00:00 +0900
categories: [ad-tech, openrtb]
---

> DSP가 "이 광고로 입찰합니다"라고 응답하는 BidResponse 구조에 대한 문서.

> **💡 Tip:** BidRequest가 "이런 광고 공간이 있어요"라는 안내문이라면,
> BidResponse는 "이 광고를 보여주고 싶어요, 이만큼 낼게요"라는 **입찰서**입니다.
>
> 입찰서에 적힌 핵심 내용:
> - **impid**: "안내문의 몇 번 공간에 입찰합니다" (요청과 응답을 연결하는 열쇠)
> - **price**: "1,000회 노출당 2,500원 낼게요" (CPM 단위)
> - **adm**: "이것이 실제 광고입니다" (이미지, 영상, 또는 HTML)
> - **mtype**: "이 광고는 배너/영상/오디오/네이티브 중 이것입니다"
>
> 하나의 DSP가 여러 광고주를 대행하므로, "나이키는 3,000원, 아디다스는 2,500원"처럼
> 광고주별로 묶어서 입찰할 수 있습니다. 이 묶음이 SeatBid입니다.

## 전체 계층 구조

```
BidResponse
├── id            (BidRequest의 id와 동일)
├── bidid         (DSP가 생성한 응답 ID, 선택)
├── cur           (통화, 기본값 USD)
├── nbr           (No-Bid 이유 코드, No-Bid 시)
├── ext           (확장 필드)
└── seatbid[]
    ├── seat      (DSP 시트 ID)
    ├── group     (묶음 낙찰 여부)
    ├── ext
    └── bid[]
        ├── id        (입찰 ID)
        ├── impid     (대응하는 imp.id)
        ├── price     (CPM 입찰가)
        ├── adid      (광고 ID)
        ├── adm       (광고 마크업)
        ├── nurl      (Win Notice URL)
        ├── burl      (Billing Notice URL)
        ├── lurl      (Loss Notice URL)
        ├── adomain[] (광고주 도메인)
        ├── cid       (캠페인 ID)
        ├── crid      (크리에이티브 ID)
        ├── mtype     (미디어 타입)
        ├── dealid    (PMP Deal ID)
        ├── w, h      (광고 크기)
        ├── exp       (입찰 만료 시간, 초)
        └── ext
```

## BidResponse 레벨 필드

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `id` | string | 필수 | BidRequest의 `id`와 동일한 값 |
| `seatbid` | object[] | 조건부 | 입찰 목록. 낙찰 의사가 있을 때 포함 |
| `bidid` | string | 선택 | DSP가 자체 생성한 응답 추적 ID |
| `cur` | string | 선택 | 입찰 통화. 기본값 `"USD"` |
| `nbr` | int | 선택 | No-Bid 이유 코드. No-Bid 응답 시 사용 |
| `ext` | object | 선택 | 확장 필드 |

## SeatBid 레벨 필드

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `bid` | object[] | 필수 | 실제 입찰 목록 |
| `seat` | string | 권장 | 구매자 시트 ID. DSP 내 광고주/계정 구분 |
| `group` | int | 선택 | `1`이면 이 seatbid의 모든 bid가 묶음으로 낙찰되어야 함 |
| `ext` | object | 선택 | 확장 필드 |

## Bid 레벨 필드

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `id` | string | 필수 | 이 입찰의 고유 ID (DSP 생성) |
| `impid` | string | 필수 | BidRequest의 `imp[].id`와 매칭 |
| `price` | float | 필수 | **CPM** 입찰가 |
| `adid` | string | 선택 | 광고 ID (사전 등록된 광고 참조) |
| `adm` | string | 권장 | 광고 마크업 (VAST XML, HTML, Native JSON 등) |
| `nurl` | string | 선택 | Win Notice URL. 낙찰 시 SSP가 서버사이드 호출 |
| `burl` | string | 선택 | Billing Notice URL. 과금 이벤트 시 SSP가 호출 |
| `lurl` | string | 선택 | Loss Notice URL. 낙찰 실패 시 SSP가 서버사이드 호출 |
| `adomain` | string[] | 권장 | 광고주 도메인 목록 (블록리스트 검사에 사용) |
| `cid` | string | 선택 | 캠페인 ID |
| `crid` | string | 권장 | 크리에이티브 ID |
| `mtype` | int | 권장 | 미디어 타입 (OpenRTB 2.6 추가) |
| `dealid` | string | 선택 | PMP Deal ID. Deal 입찰 시 BidRequest의 deal.id와 매칭 |
| `w` | int | 권장 | 광고 너비 (픽셀) |
| `h` | int | 권장 | 광고 높이 (픽셀) |
| `exp` | int | 선택 | 입찰 만료 시간 (초). 기본값 3600 |
| `ext` | object | 선택 | 확장 필드 |

## mtype 값 (OpenRTB 2.6)

| 값 | 의미 | 설명 |
|----|------|------|
| 1 | Banner | HTML/이미지 배너 광고 |
| 2 | Video | VAST XML 또는 VAST URL |
| 3 | Audio | DAAST XML 또는 오디오 스트림 |
| 4 | Native | Native 광고 JSON |

## price 필드

OpenRTB 스펙에서 `price`는 **CPM (Cost Per Mille)** 단위로 명시되어 있다.

```
price: 2.5  →  $2.5 per 1,000 impressions
```

프로그래매틱 광고 업계에서 CPM이 표준 단위인 이유:
- 노출 기반 과금이 기본
- 1,000회 노출 단위가 계산/비교에 용이
- RTB 경매에서 실시간 비교 가능

## nurl / burl / lurl 비교

> **💡 Tip:**
> - **nurl** = 경매에서 망치를 내리며 "낙찰!" 외치는 순간. 아직 돈을 내지 않았습니다.
> - **burl** = 실제로 계좌에서 돈이 빠져나가는 순간. 광고가 진짜 사용자에게 보여진 후입니다.
> - **lurl** = "아쉽지만 이번에는 떨어졌습니다" 통보. 왜 떨어졌는지 사유도 알려줍니다.
>
> 중요: nurl(낙찰 알림)과 burl(과금)은 완전히 다른 시점입니다!
> 낙찰되었다고 바로 돈을 내는 게 아니라, 광고가 실제로 화면에 보여진 후에 과금합니다.

| 필드 | 트리거 시점 | 호출 주체 | 용도 |
|------|------------|----------|------|
| `nurl` | 낙찰 확정 직후 | SSP 서버 | Win 알림. 가격 매크로 치환 포함 |
| `burl` | 실제 과금 이벤트 발생 시 | SSP 서버 | 청구 확정 알림. nurl과 시점 다를 수 있음 |
| `lurl` | 낙찰 실패 시 | SSP 서버 | Loss 알림. 경쟁 낙찰가 등 전달 가능 |

> **⚠️ Warning:** `nurl`은 낙찰이 확정된 시점에 호출되고, `burl`은 실제 광고가 노출되거나 과금이 확정된 시점에 호출된다. 광고 사기 방지 및 정확한 정산을 위해 두 개를 별도로 운용하는 DSP가 많다.

## nurl 매크로

nurl/burl/lurl에는 매크로가 포함되어 SSP가 호출 시 실제 값으로 치환한다.

```
https://dsp.com/win?price=${AUCTION_PRICE}&bid=${AUCTION_BID_ID}
           ↓ SSP가 치환
https://dsp.com/win?price=2.11&bid=bid-123
```

| 매크로 | 의미 |
|--------|------|
| `${AUCTION_PRICE}` | 실제 낙찰가 (과금가) |
| `${AUCTION_BID_ID}` | 입찰 ID |
| `${AUCTION_IMP_ID}` | Impression ID |
| `${AUCTION_SEAT_ID}` | Seat ID |
| `${AUCTION_AD_ID}` | 광고 ID |
| `${AUCTION_LOSS}` | Loss 이유 코드 (lurl 전용) |
| `${AUCTION_CURRENCY}` | 통화 |

## No-Bid 응답

DSP가 입찰하지 않을 경우 두 가지 방식으로 응답한다.

**방법 1: HTTP 204 No Content**
가장 단순한 방법. 응답 본문 없이 204 상태 코드만 반환한다.

**방법 2: HTTP 200 + nbr 코드**
```json
{
  "id": "req-123",
  "nbr": 2
}
```

## nbr (No-Bid Reason) 코드

| 값 | 의미 |
|----|------|
| 0 | Unknown Error |
| 1 | Technical Error |
| 2 | Invalid Request |
| 3 | Known Web Spider |
| 4 | Suspected Non-Human Traffic |
| 5 | Cloud/Data Center/Proxy IP |
| 6 | Unsupported Device |
| 7 | Blocked Publisher or Site |
| 8 | Unmatched User |
| 9 | Daily Reader Cap Met |
| 10 | Daily Domain Cap Met |

## 실전 예시

### Banner 입찰 응답

```json
{
  "id": "req-banner-123",
  "bidid": "dsp-resp-456",
  "cur": "USD",
  "seatbid": [{
    "seat": "seat-001",
    "bid": [{
      "id": "bid-001",
      "impid": "imp-1",
      "price": 1.75,
      "adm": "<div>...배너 HTML...</div>",
      "adomain": ["advertiser.com"],
      "cid": "campaign-99",
      "crid": "creative-banner-01",
      "mtype": 1,
      "w": 300,
      "h": 250,
      "nurl": "https://dsp.com/win?price=${AUCTION_PRICE}&bid=${AUCTION_BID_ID}",
      "burl": "https://dsp.com/bill?price=${AUCTION_PRICE}"
    }]
  }]
}
```

### Video 입찰 응답 (VAST in adm)

```json
{
  "id": "req-video-123",
  "cur": "USD",
  "seatbid": [{
    "seat": "seat-001",
    "bid": [{
      "id": "bid-video-001",
      "impid": "imp-video-1",
      "price": 4.50,
      "adm": "<?xml version=\"1.0\"?><VAST version=\"4.0\">...</VAST>",
      "adomain": ["brand.com"],
      "crid": "creative-video-01",
      "mtype": 2,
      "w": 1280,
      "h": 720,
      "nurl": "https://dsp.com/win?price=${AUCTION_PRICE}",
      "burl": "https://dsp.com/bill?price=${AUCTION_PRICE}",
      "lurl": "https://dsp.com/loss?reason=${AUCTION_LOSS}"
    }]
  }]
}
```

### No-Bid 응답 (nbr 포함)

```json
{
  "id": "req-123",
  "nbr": 8
}
```

### Multi-Seat 응답

하나의 BidResponse에 여러 Seat(광고주/계정)가 각각 입찰하는 경우.

```json
{
  "id": "req-multi-123",
  "cur": "USD",
  "seatbid": [
    {
      "seat": "seat-brand-a",
      "bid": [{
        "id": "bid-a-001",
        "impid": "imp-1",
        "price": 3.20,
        "adm": "...",
        "adomain": ["brand-a.com"],
        "crid": "creative-a-01",
        "mtype": 1
      }]
    },
    {
      "seat": "seat-brand-b",
      "bid": [{
        "id": "bid-b-001",
        "impid": "imp-1",
        "price": 2.80,
        "adm": "...",
        "adomain": ["brand-b.com"],
        "crid": "creative-b-01",
        "mtype": 1
      }]
    }
  ]
}
```

## 검증 체크리스트

백엔드 개발자가 BidResponse 파싱 및 처리 로직을 구현할 때 확인해야 할 항목.

**필수 필드 검증**
- [ ] `id`가 BidRequest의 `id`와 일치하는가
- [ ] `seatbid[].bid[].impid`가 BidRequest의 `imp[].id` 중 하나와 매칭되는가
- [ ] `price`가 0 이상의 유효한 숫자인가
- [ ] `price`가 해당 imp의 `bidfloor`를 충족하는가

**광고 마크업 검증**
- [ ] `adm` 또는 사전 등록 `adid` 중 하나가 반드시 있는가
- [ ] `mtype`과 `adm` 내용이 일치하는가 (비디오 imp에 Banner adm 혼입 방지)
- [ ] `adomain`이 블록리스트에 없는가

**URL 처리**
- [ ] `nurl` 호출은 낙찰 확정 직후 서버사이드에서 수행하는가
- [ ] `nurl` 호출 전 매크로(`${AUCTION_PRICE}` 등)를 실제 값으로 치환했는가
- [ ] `burl`과 `nurl`의 호출 시점을 정확히 구분하고 있는가
- [ ] `lurl`은 비낙찰 시 서버사이드에서 호출하는가

**PMP Deal 처리**
- [ ] Deal 입찰인 경우 `dealid`가 BidRequest의 `imp[].pmp.deals[].id`와 매칭되는가
- [ ] Deal 입찰가가 `deal.bidfloor`를 충족하는가

**No-Bid 처리**
- [ ] HTTP 204와 `nbr` 포함 200 응답 모두 No-Bid로 처리하는가
- [ ] `seatbid`가 빈 배열인 경우도 No-Bid로 처리하는가
