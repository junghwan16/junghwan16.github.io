---
layout: single
title: "OpenRTB BidRequest Video 객체 완전 정리"
date: 2026-02-24 17:00:00 +0900
categories: [ad-tech, openrtb]
---

> SSP가 DSP에게 "이런 비디오 광고 지면이 있어요"라고 알려주는 BidRequest의 Video 객체에 대한 문서.

> **💡 Tip:** 영화관에서 영화가 시작하기 전에 광고가 나오죠? 그것이 비디오 광고입니다.
> BidRequest의 Video 객체는 "이런 영상 광고 공간이 있어요"라고 알리는 안내문입니다.
> 안내문에는 이런 내용이 적혀 있습니다:
> - "영상 길이는 5초~30초 사이로 해주세요" (minduration/maxduration)
> - "mp4 형식만 가능해요" (mimes)
> - "영화 시작 전에 나와요" (startdelay=0, 프리롤)
> - "5초 후에 건너뛰기 가능해요" (skip=1, skipafter=5)
> - "화면 크기는 640x360이에요" (w/h)

## Video 객체 구조

```json
{
  "imp": [{
    "id": "imp-1",
    "video": {
      "mimes": ["video/mp4", "video/webm"],
      "minduration": 5,
      "maxduration": 30,
      "protocols": [2, 3, 5, 6, 7, 8],
      "w": 1280,
      "h": 720,
      "linearity": 1,
      "plcmt": 1,
      "startdelay": 0,
      "skip": 1,
      "skipmin": 5,
      "skipafter": 5,
      "playbackmethod": [2],
      "api": [1, 2]
    }
  }]
}
```

## 주요 필드

| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| `mimes` | string[] | 권장 | 지원하는 MIME 타입 (video/mp4 등) |
| `minduration` | int | 권장 | 최소 광고 길이 (초) |
| `maxduration` | int | 권장 | 최대 광고 길이 (초) |
| `protocols` | int[] | 권장 | 지원하는 VAST 버전 목록 |
| `w` | int | 권장 | 비디오 너비 (픽셀) |
| `h` | int | 권장 | 비디오 높이 (픽셀) |
| `linearity` | int | 선택 | 광고 유형 (Linear / Non-linear) |
| `plcmt` | int | 권장 | 광고 배치 유형 (OpenRTB 2.6, `placement` 대체) |
| `placement` | int | Deprecated | OpenRTB 2.6에서 `plcmt`로 대체됨 |
| `startdelay` | int | 권장 | 광고 시작 시점 (프리롤/미드롤/포스트롤 구분) |
| `skip` | int | 선택 | 스킵 허용 여부 (1=허용, 0=불허) |
| `skipmin` | int | 선택 | 스킵 버튼 표시를 위한 최소 광고 길이 (초) |
| `skipafter` | int | 선택 | 광고 시작 후 스킵 가능 시점 (초) |
| `playbackmethod` | int[] | 선택 | 재생 방식 목록 |
| `api` | int[] | 선택 | 지원하는 API 프레임워크 |
| `podid` | string | 선택 | Ad Pod 식별자 (OpenRTB 2.6, CTV 컨텍스트) |
| `podseq` | int | 선택 | Pod 내 순서 (0=Any, 1=First, -1=Last) |
| `slotinpod` | int | 선택 | Pod 내 슬롯 위치 (0=Any, 1=First, -1=Last) |

## protocols 값

| 값 | 의미 |
|----|------|
| 1 | VAST 1.0 |
| 2 | VAST 2.0 |
| 3 | VAST 3.0 |
| 4 | VAST 1.0 Wrapper |
| 5 | VAST 2.0 Wrapper |
| 6 | VAST 3.0 Wrapper |
| 7 | VAST 4.0 |
| 8 | VAST 4.0 Wrapper |

## plcmt 값 (OpenRTB 2.6)

> **⚠️ Warning:** `placement` 필드는 2.6에서 deprecated 처리되었으며, `plcmt`가 공식 대체 필드다. 신규 연동 시 반드시 `plcmt`를 사용해야 한다.

| 값 | 이름 | 설명 |
|----|------|------|
| 1 | Instream | 프리롤/미드롤/포스트롤. 콘텐츠 재생 전후에 삽입. 시청자가 의도적으로 콘텐츠를 선택한 상황 |
| 2 | Accompanying Content | 콘텐츠와 함께 표시되는 아웃스트림. 콘텐츠 옆에 고정되거나 스크롤 트리거 |
| 3 | Interstitial | 전면 광고. 콘텐츠 전환 시점에 전체 화면으로 노출 |
| 4 | No Content / Standalone | 독립형. 비디오 콘텐츠 없이 광고만 단독으로 존재 |

## placement 값 (Deprecated in 2.6)

| 값 | 의미 | 설명 |
|----|------|------|
| 1 | In-Stream | 프리롤, 미드롤, 포스트롤 |
| 2 | In-Banner | 배너 영역에서 재생 |
| 3 | In-Article | 기사 본문 내 |
| 4 | In-Feed | 피드 내 |
| 5 | Interstitial/Floating | 전면/플로팅 |

## linearity 값

| 값 | 의미 | 설명 |
|----|------|------|
| 1 | Linear | 인스트림. 콘텐츠 전/중/후 재생 |
| 2 | Non-linear | 오버레이. 콘텐츠 위에 표시 |

## startdelay 값

| 값 | 의미 | 설명 |
|----|------|------|
| `0` | Pre-Roll | 콘텐츠 시작 전 재생 |
| `>0` | Mid-Roll | 콘텐츠 중간 재생. 값 = 시작 시간(초) |
| `-1` | Generic Mid-Roll | 미드롤이지만 정확한 시점 미지정 |
| `-2` | Generic Post-Roll | 포스트롤이지만 정확한 시점 미지정 |

## skip 관련 필드

| 필드 | 예시 값 | 설명 |
|------|---------|------|
| `skip` | `1` | 스킵 허용 여부. 0=불허, 1=허용 |
| `skipmin` | `15` | 이 값(초) 이상의 광고에만 스킵 버튼 표시 |
| `skipafter` | `5` | 광고 시작 후 몇 초 뒤에 스킵 가능한지 |

> **ℹ️ Note:** 리워드 광고는 일반적으로 `skip: 0`으로 설정하여 광고를 끝까지 시청해야 보상이 지급되게 한다.

## playbackmethod 값

| 값 | 의미 |
|----|------|
| 1 | Autoplay, Sound On |
| 2 | Autoplay, Sound Off (기본값으로 가장 흔히 사용) |
| 3 | Click-to-Play |
| 4 | Mouse-Over |
| 5 | Entering Viewport, Sound On |
| 6 | Entering Viewport, Sound Off |

## Ad Pods 필드 (OpenRTB 2.6, CTV 컨텍스트)

CTV(Connected TV) 환경에서 여러 광고를 연속으로 재생하는 광고 블록을 Ad Pod라고 한다. 하나의 광고 휴식 시간에 여러 슬롯이 포함될 수 있으며, 각 슬롯은 별도의 BidRequest로 전달된다.

| 필드 | 타입 | 설명 |
|------|------|------|
| `podid` | string | 같은 Ad Pod 내 슬롯들이 공유하는 Pod 식별자 |
| `podseq` | int | Pod가 광고 휴식 시간 내에서 몇 번째인지. 0=Any, 1=First, 2=Second, -1=Last |
| `slotinpod` | int | Pod 내에서 이 슬롯이 몇 번째인지. 0=Any, 1=First, -1=Last |

## 실전 예시

### Pre-Roll 비디오 요청

```json
{
  "imp": [{
    "id": "imp-preroll-1",
    "video": {
      "mimes": ["video/mp4"],
      "minduration": 15,
      "maxduration": 30,
      "protocols": [2, 3, 5, 6, 7, 8],
      "w": 1280,
      "h": 720,
      "linearity": 1,
      "plcmt": 1,
      "startdelay": 0,
      "skip": 1,
      "skipmin": 15,
      "skipafter": 5,
      "playbackmethod": [2]
    }
  }]
}
```

### Rewarded 비디오 요청

리워드 광고는 `imp` 레벨에 `rwdd: 1`을 설정하고, 비디오는 스킵 불가로 설정한다.

```json
{
  "imp": [{
    "id": "imp-reward-1",
    "rwdd": 1,
    "video": {
      "mimes": ["video/mp4"],
      "minduration": 15,
      "maxduration": 30,
      "protocols": [2, 3, 5, 6, 7, 8],
      "w": 640,
      "h": 480,
      "linearity": 1,
      "plcmt": 3,
      "startdelay": 0,
      "skip": 0,
      "playbackmethod": [3]
    }
  }]
}
```

### Outstream 비디오 요청

아웃스트림은 `plcmt: 4` (No Content / Standalone) 또는 `plcmt: 2` (Accompanying Content)를 사용한다.

```json
{
  "imp": [{
    "id": "imp-outstream-1",
    "video": {
      "mimes": ["video/mp4"],
      "minduration": 5,
      "maxduration": 60,
      "protocols": [2, 3, 5, 6, 7, 8],
      "w": 640,
      "h": 360,
      "linearity": 1,
      "plcmt": 4,
      "skip": 1,
      "skipmin": 5,
      "skipafter": 3,
      "playbackmethod": [2, 6]
    }
  }]
}
```

## 실무 포인트

**duration 협의 필수**
`minduration`/`maxduration`은 DSP가 보유한 크리에이티브 길이와 직접 매칭된다. 범위가 너무 좁으면 Fill Rate가 낮아지므로 운영 매니저와 사전 협의가 필요하다.

**plcmt vs placement 혼용 주의**
OpenRTB 2.6 이전 스펙을 사용하는 DSP 파트너는 여전히 `placement`를 읽을 수 있다. 하위 호환성을 위해 두 필드를 모두 전송하는 SSP도 있다. 연동 스펙 확인 필수.

**startdelay로 광고 유형 분류**
프리롤/미드롤/포스트롤 구분은 `startdelay` 값으로 한다. DSP는 이 값을 기반으로 캠페인 타겟팅 조건을 적용한다.

**playbackmethod 자동재생 정책**
모바일 환경에서 자동재생 광고는 대부분 음소거(`playbackmethod: 2`) 상태로 시작한다. 오디오 광고 인벤토리가 아니면 `1`(Sound On) 자동재생은 브라우저 정책에 의해 차단될 수 있다.

**CTV Ad Pod**
CTV 연동 시 `podid`, `podseq`, `slotinpod`을 정확히 설정해야 광고 블록 내 경쟁 분리(competitive separation)와 주파수 제한이 올바르게 동작한다.
